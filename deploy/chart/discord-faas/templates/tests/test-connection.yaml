apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "discord-faas.fullname" . }}-test-connection"
  labels:
    {{- include "discord-faas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "discord-faas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
